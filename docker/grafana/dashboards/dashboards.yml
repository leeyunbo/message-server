apiVersion: 1

providers:
  - name: ReadTimeout Dashboards
    orgId: 1
    folder: ReadTimeout
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
